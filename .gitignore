node_modules/
data/
.env*
test-artifacts/

# generated at run time (added 2026-09-14 before the first commit)
*.log
.env
.env.local
.DS_Store
public/slides/*
!public/slides/how-vivek-bot-works.png
tools/latency/runs/
tools/latency/clips/
tools/latency/*.wav
tools/latency/*.png
tools/latency/run-*.json
tools/latency/obs-run-*.json
tools/latency/realtime/run-*.json
tools/latency/meet-live-fixture-result.json
__pycache__/

# Behavioral test loop: full-desktop screen recordings and the frames cut from them capture unrelated windows.
# Kept locally; the annotated summary videos (cropped to the meeting) are committed.
tools/present-lab/runs/**/*_recording.mp4
tools/present-lab/runs/**/frame_*.png
# Telegram audit log (chat, user and attachment ids): local only.
Telegram_calls.md

# Simulated-participant runs (added 2026-09-23): raw audio, rendered videos and demo build intermediates are
# regenerated from the run JSON; kept locally (about 550 MB). Reviews, transcripts and evidence images are committed.
tools/sim-participant/runs/**/*.pcm
tools/sim-participant/runs/**/*.wav
tools/sim-participant/runs/**/*.opus
tools/sim-participant/runs/**/*.mp4
tools/sim-participant/runs/**/built/
