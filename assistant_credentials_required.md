# Assistant credentials required — RoboMeet Google Meet sign-in

Written by Claude Code, 2026-09-13. Everything Claude could fill in is filled in.
Exactly ONE field was yours: the password. DONE 2026-09-13. Nothing further is needed from you for sign-in.

## The one thing you provide

| Field | Value | Who |
| --- | --- | --- |
| `assistant_google_password` | (a NEW password you set — see Steps) | **YOU** |

## Everything else (filled by Claude)

| Field | Value | Source |
| --- | --- | --- |
| `assistant_google_email` | `vivekkmk.assistant@gmail.com` | CLAUDE.md, context anchor |
| `account_type` | Personal Gmail (not Google Workspace) | Google account UI |
| `meet_display_name` | Vivek Bot | Host window, 2026-09-13 |
| `recovery_email` | `viv•••••••••••@gmail.com` (Google masks it; your personal Gmail, signed in on your phone) | Google recovery screen, 2026-09-13 |
| `recovery_phone` | none offered by Google's recovery screen, so apparently not set | Google recovery screen |
| `two_step_verification` | apparently OFF (no 2SV challenge appeared, only "verify it's you" options) | Google recovery screen |
| `passkey` | none observed | Google recovery screen |
| `authuser_index_in_regular_chrome` | 2 | Host tab URL `?authuser=2` |
| `regular_chrome_session` | signed in as the assistant account now; a password change will sign it out once (Google rule). gws API access is unaffected | support.google.com/accounts/answer/41078 |
| `robot_profile_signed_in` | YES (signed in by Vivek 10:25 PM machine time; robot joined the meeting as Vivek Bot at 10:33 PM) | cookie names in data/browser-profile/Default/Cookies |
| `robot_chrome_profile` | `/home/vivekkarmarkar/Documents/Codex/2026-09-11/okay-so-i-have-an-interesting/outputs/robomeet/data/browser-profile` — created by the app, never signed in, persistent (stays signed in after one login; the app never clears it) | src/browser-profile.mjs, src/meet-worker.mjs |
| `robot_signin_window` | closed after sign-in. Was: open via `node bin/login.mjs` (relaunched 10:17 PM machine time, Chrome pid 2573552), at `https://accounts.google.com/ServiceLogin?Email=vivekkmk.assistant%40gmail.com&continue=https%3A%2F%2Fmeet.google.com%2F`, email already filled, waiting at the password box. If it is gone, run `node bin/login.mjs` in this folder to reopen it | Display :1 |
| `password_change_window` | not open any more (your regular Chrome was relaunched). Reset the password from your phone (Option A) or say "send it" and Claude re-stages it | Display :1 |
| `test_meeting` | `https://meet.google.com/onh-rhid-zuu` — API-created, accessType OPEN, entryPointAccess ALL, organizer = the assistant account (`spaces/mJrUzJy2S2QB`) | gws meet spaces get, 2026-09-13 |
| `gws_meet_oauth` | done; config `data/gws-meet-auth`, scope `meetings.space.created` | context anchor 01:39 CDT |
| `openai_api_key` | present server-side (existing env file); no action | README, context anchor |
| `bridge_secret` | generated by `node bin/bridge-build.mjs` only if the extension path is ever used; no action | bin/bridge-build.mjs |
| `app_start_command_after_signin` | `ROBOMEET_PROFILE_DIR=data/browser-profile npm start` (Claude runs this) | README |

## Steps — minimum donkey work

### Option A — reset on your phone now, 30 seconds at the laptop later (recommended)

1. On your phone, open `https://accounts.google.com`, enter `vivekkmk.assistant@gmail.com`, tap **Forgot password?**, choose **Tap Yes on the device your recovery email is signed into** (that is this phone), tap **Yes**, set a new password. No laptop needed. Google will sign the assistant account out of your regular Chrome; that is expected.
2. Next time you are at the laptop: in the **left** Chrome window (robot profile, email already filled) type the new password, press **Next**, close that window once Meet's home page shows. About 30 seconds.
3. Tell Claude "done". Claude does everything after that.

### Option B — everything at the laptop, about 2 minutes

1. Tell Claude "send it". Claude triggers the phone prompt from the right window.
2. Tap **Yes** on the phone.
3. Right window: type the new password twice.
4. Left window: type it once, press **Next**, close the window.

## What Claude does after step "done" (no action from you)

1. Start the app on the signed-in profile with voice OFF: `ROBOMEET_PROFILE_DIR=data/browser-profile npm start` (on a second port if 4318 is busy).
2. Join `onh-rhid-zuu` as the robot; since the robot is signed in as the meeting's organizer, no admission is needed.
3. Verify admission, audio both ways, robot camera, screen share. Report exactly what Google shows.
4. Only if you say so afterwards: a short paid GPT Live test.

## Is this recurring?

No. The robot profile is persistent and the app never signs out or clears it, so every later meeting reuses the signed-in profile with no sign-in. Only if Google ever invalidates the saved session (vendors report this as rare) would step 2 of Option A repeat.
